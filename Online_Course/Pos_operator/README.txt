"write a program that reads the number of points (integer)  and the step (real) and builds the position operator"
"a matrix rhat acting on a "delta" gives the delta times the position"
"delta in a finite basis set:"
